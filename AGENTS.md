## About

- magento module
- add `Search by AI` feature on every grid in admin panel
- applicable just for ui component grids, not legacy
- apply filters, sorting, columns visibility on the grid using natural language via LLM

## Structure

- `root` - contains readme, composer, etc files
- `src` - dir contains magento module

